# Modo apresentação: REST via Gateway; WebSocket direto no browser (evita proxy duplo do Vite)
VITE_USE_GATEWAY=true
VITE_WS_BASE=http://localhost:8080/ws

# Se o Gateway ainda instável com SockJS, use só o chat direto (REST continua via Gateway):
# VITE_WS_BASE=http://localhost:8082/ws
